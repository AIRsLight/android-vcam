diff --git a/common/apex_update_listener/Android.bp b/common/apex_update_listener/Android.bp
index 377b99a..6e8b8f7 100644
--- a/common/apex_update_listener/Android.bp
+++ b/common/apex_update_listener/Android.bp
@@ -31,5 +31,5 @@ cc_defaults {
   static_libs: [
     "libtinyxml2",
-    "libapex_update_listener",
+    "//hardware/google/camera/common/apex_update_listener:libapex_update_listener",
   ],
 }
diff --git a/common/hal/aidl_service/Android.bp b/common/hal/aidl_service/Android.bp
index df5d70e..65930b1 100644
--- a/common/hal/aidl_service/Android.bp
+++ b/common/hal/aidl_service/Android.bp
@@ -69,12 +69,6 @@ cc_genrule {
 }
 
-cc_defaults {
-    name: "camera_service_defaults_common",
-    defaults: [
-        "google_camera_hal_defaults",
-        "apex_update_listener_cc_defaults_static",
-    ],
-    vendor: true,
-    relative_install_path: "hw",
+filegroup {
+    name: "camera_service_common_sources",
     srcs: [
         "aidl_camera_device.cc",
@@ -86,6 +80,22 @@ cc_defaults {
         "libc_wrappers.cc",
     ],
+}
+
+filegroup {
+    name: "camera_service_main_source",
+    srcs: ["aidl_service.cc"],
+}
+
+cc_defaults {
+    name: "camera_service_defaults_common",
+    defaults: [
+        "google_camera_hal_defaults",
+        "apex_update_listener_cc_defaults_static",
+    ],
+    vendor: true,
+    relative_install_path: "hw",
+    srcs: ["//hardware/google/camera/common/hal/aidl_service:camera_service_common_sources"],
     generated_headers: [
-        "aidl_camera_build_version",
+        "//hardware/google/camera/common/hal/aidl_service:aidl_camera_build_version",
     ],
     compile_multilib: "first",
@@ -104,13 +114,13 @@ cc_defaults {
         "libcutils",
         "libfmq",
-        "libgooglecamerahal",
-        "libgooglecamerahalutils",
+        "//hardware/google/camera/common/hal/google_camera_hal:libgooglecamerahal",
+        "//hardware/google/camera/common/hal/utils:libgooglecamerahalutils",
         "libhidlbase",
         "liblog",
         "libutils",
-        "lib_profiler",
+        "//hardware/google/camera/common/profiler:lib_profiler",
     ],
     export_shared_lib_headers: [
-        "lib_profiler",
+        "//hardware/google/camera/common/profiler:lib_profiler",
     ],
     static_libs: [
@@ -124,10 +134,19 @@ cc_defaults {
         "camera_service_defaults_common",
     ],
-    srcs: [
-        "aidl_service.cc",
-    ],
+    srcs: ["//hardware/google/camera/common/hal/aidl_service:camera_service_main_source"],
     vintf_fragments: [":android.hardware.camera.provider@2.7-service-google.xml"],
 }
 
+// Transport defaults for the collision-free android-vcam provider. The
+// product-owned binary supplies its own init and VINTF fragments.
+cc_defaults {
+    name: "camera_service_vcam_defaults",
+    defaults: [
+        "camera_service_defaults_common",
+    ],
+    srcs: ["//hardware/google/camera/common/hal/aidl_service:camera_service_main_source"],
+    cflags: ["-DANDROID_VCAM_PROVIDER"],
+}
+
 cc_binary {
     name: "android.hardware.camera.provider@2.7-service-google",
diff --git a/common/hal/aidl_service/aidl_camera_provider.cc b/common/hal/aidl_service/aidl_camera_provider.cc
index 7eac3f2..cb798da 100644
--- a/common/hal/aidl_service/aidl_camera_provider.cc
+++ b/common/hal/aidl_service/aidl_camera_provider.cc
@@ -41,8 +41,15 @@ using aidl::android::hardware::camera::common::VendorTagSection;
 using ::android::google_camera_hal::CameraDevice;
 
+#ifdef ANDROID_VCAM_PROVIDER
+const std::string AidlCameraProvider::kProviderName = "vcam";
+// "device@<version>/vcam/<id>"
+const std::regex AidlCameraProvider::kDeviceNameRegex(
+    "device@([0-9]+\\.[0-9]+)/vcam/(.+)");
+#else
 const std::string AidlCameraProvider::kProviderName = "internal";
 // "device@<version>/internal/<id>"
 const std::regex AidlCameraProvider::kDeviceNameRegex(
     "device@([0-9]+\\.[0-9]+)/internal/(.+)");
+#endif
 
 std::shared_ptr<AidlCameraProvider> AidlCameraProvider::Create() {
diff --git a/common/hal/aidl_service/aidl_service.cc b/common/hal/aidl_service/aidl_service.cc
index 91f52b1..a1e2478 100644
--- a/common/hal/aidl_service/aidl_service.cc
+++ b/common/hal/aidl_service/aidl_service.cc
@@ -32,4 +32,5 @@
 
 #include <cinttypes>
+#include <cstdlib>
 
 #include "aidl_camera_build_version.h"
@@ -45,5 +46,9 @@ const bool kLazyService = false;
 #endif
 
+#ifdef ANDROID_VCAM_PROVIDER
+const std::string kProviderInstance = "/vcam/0";
+#else
 const std::string kProviderInstance = "/internal/0";
+#endif
 
 int main() {
@@ -61,4 +66,11 @@ int main() {
   ABinderProcess_startThreadPool();
 
+#ifdef ANDROID_VCAM_PROVIDER
+  if (setenv("ANDROID_CAMERA_HWL_LIBRARY",
+             "libvcam_googlecamerahwl_impl.so", 1) != 0) {
+    ALOGE("Unable to select the android-vcam HWL library");
+    return android::NO_INIT;
+  }
+#endif
 #ifdef __ANDROID_APEX__
   int start_count = property_get_int32("vendor.camera.hal.start.count", 0);
diff --git a/common/hal/google_camera_hal/camera_provider.cc b/common/hal/google_camera_hal/camera_provider.cc
index 30e184d..6242e0d 100644
--- a/common/hal/google_camera_hal/camera_provider.cc
+++ b/common/hal/google_camera_hal/camera_provider.cc
@@ -21,4 +21,5 @@
 
 #include <dlfcn.h>
+#include <cstdlib>
 #include <log/log.h>
 #include <utils/Trace.h>
@@ -33,4 +34,6 @@
 // HWL layer implementation path
 constexpr std::string_view kCameraHwlLib = "libgooglecamerahwl_impl.so";
+constexpr std::string_view kCameraHwlLibEnvironment =
+    "ANDROID_CAMERA_HWL_LIBRARY";
 #endif
 
@@ -305,9 +308,14 @@ status_t CameraProvider::CreateHwl(
   CreateCameraProviderHwl_t create_hwl;
 
-  ALOGI("%s:Loading %s library", __FUNCTION__, kCameraHwlLib.data());
-  hwl_lib_handle_ = dlopen(kCameraHwlLib.data(), RTLD_NOW);
+  const char* selected_hwl = std::getenv(kCameraHwlLibEnvironment.data());
+  if (selected_hwl == nullptr || selected_hwl[0] == '\0' ||
+      strchr(selected_hwl, '/') != nullptr) {
+    selected_hwl = kCameraHwlLib.data();
+  }
+  ALOGI("%s: Loading %s library", __FUNCTION__, selected_hwl);
+  hwl_lib_handle_ = dlopen(selected_hwl, RTLD_NOW);
 
   if (hwl_lib_handle_ == nullptr) {
-    ALOGE("HWL loading %s failed due to error: %s", kCameraHwlLib.data(),
+    ALOGE("HWL loading %s failed due to error: %s", selected_hwl,
           dlerror());
     return NO_INIT;
@@ -317,5 +325,5 @@ status_t CameraProvider::CreateHwl(
                                                 "CreateCameraProviderHwl");
   if (create_hwl == nullptr) {
-    ALOGE("%s: dlsym failed (%s).", __FUNCTION__, kCameraHwlLib.data());
+    ALOGE("%s: dlsym failed (%s).", __FUNCTION__, selected_hwl);
     dlclose(hwl_lib_handle_);
     hwl_lib_handle_ = nullptr;
diff --git a/devices/EmulatedCamera/hwl/Android.bp b/devices/EmulatedCamera/hwl/Android.bp
index fad5ef0..2c620be 100644
--- a/devices/EmulatedCamera/hwl/Android.bp
+++ b/devices/EmulatedCamera/hwl/Android.bp
@@ -4,8 +4,6 @@ package {
 }
 
-cc_defaults {
-    name: "libgooglecamerahwl_impl_defaults",
-    owner: "google",
-    proprietary: true,
+filegroup {
+    name: "emulated_camera_hwl_sources",
     srcs: [
         "EmulatedCameraProviderHWLImpl.cpp",
@@ -18,4 +16,11 @@ cc_defaults {
         "GrallocSensorBuffer.cpp",
     ],
+}
+
+cc_defaults {
+    name: "libgooglecamerahwl_impl_defaults",
+    owner: "google",
+    proprietary: true,
+    srcs: ["//hardware/google/camera/devices/EmulatedCamera/hwl:emulated_camera_hwl_sources"],
     cflags: [
         "-Werror",
@@ -34,5 +39,5 @@ cc_defaults {
         "android.hardware.sensors@1.0",
         "android.hidl.allocator@1.0",
-        "lib_profiler",
+        "//hardware/google/camera/common/profiler:lib_profiler",
         "libbase",
         "libcamera_metadata",
@@ -42,5 +47,5 @@ cc_defaults {
         "libhardware",
         "libhidlbase",
-        "libgooglecamerahalutils",
+        "//hardware/google/camera/common/hal/utils:libgooglecamerahalutils",
         "libjpeg",
         "libjsoncpp",
@@ -52,5 +57,5 @@ cc_defaults {
     static_libs: [
         "android.hardware.camera.common@1.0-helper",
-        "libgooglecamerahwl_sensor_impl",
+        "//hardware/google/camera/devices/EmulatedCamera/hwl:libgooglecamerahwl_sensor_impl",
     ],
     include_dirs: [
@@ -58,5 +63,5 @@ cc_defaults {
     ],
     header_libs: [
-        "libgooglecamerahal_headers",
+        "//hardware/google/camera/common/hal:libgooglecamerahal_headers",
     ],
 }
diff --git a/devices/EmulatedCamera/hwl/EmulatedCameraProviderHWLImpl.h b/devices/EmulatedCamera/hwl/EmulatedCameraProviderHWLImpl.h
index e9c086b..e146ec8 100644
--- a/devices/EmulatedCamera/hwl/EmulatedCameraProviderHWLImpl.h
+++ b/devices/EmulatedCamera/hwl/EmulatedCameraProviderHWLImpl.h
@@ -97,8 +97,10 @@ class EmulatedCameraProviderHwlImpl : public CameraProviderHwl {
 };
 
+#ifndef ANDROID_VCAM_CUSTOM_FACTORY
 extern "C" CameraProviderHwl* CreateCameraProviderHwl() {
   auto provider = EmulatedCameraProviderHwlImpl::Create();
   return provider.release();
 }
+#endif
 
 }  // namespace android
diff --git a/devices/EmulatedCamera/hwl/EmulatedSensor.cpp b/devices/EmulatedCamera/hwl/EmulatedSensor.cpp
index 7d93ded..22a480c 100644
--- a/devices/EmulatedCamera/hwl/EmulatedSensor.cpp
+++ b/devices/EmulatedCamera/hwl/EmulatedSensor.cpp
@@ -45,4 +45,16 @@
 namespace android {
 
+// Optional android-vcam frame hooks. The weak linkage keeps the stock
+// EmulatedCamera behavior unchanged for every other HWL consumer.
+extern "C" void VcamSetActiveFrame(uint32_t camera_id, uint32_t frame_number)
+    __attribute__((weak));
+extern "C" bool VcamRenderYuv420(
+    uint8_t* y, uint8_t* cb, uint8_t* cr, uint32_t width, uint32_t height,
+    uint32_t y_stride, uint32_t cbcr_stride, uint32_t cbcr_step,
+    size_t bytes_per_pixel) __attribute__((weak));
+extern "C" bool VcamRenderRgb(uint8_t* image, uint32_t width, uint32_t height,
+                               uint32_t stride, uint32_t layout)
+    __attribute__((weak));
+
 using android::google_camera_hal::ErrorCode;
 using google_camera_hal::HalCameraMetadata;
@@ -795,4 +807,8 @@ bool EmulatedSensor::threadLoop() {
              device_settings->second.gain);
 
+      if (VcamSetActiveFrame != nullptr) {
+        VcamSetActiveFrame((*b)->camera_id, (*b)->frame_number);
+      }
+
       scene_->Initialize(device_chars->second.full_res_width,
                          device_chars->second.full_res_height,
@@ -1436,4 +1452,9 @@ void EmulatedSensor::CaptureRGB(uint8_t* img, uint32_t width, uint32_t height,
                                 const SensorCharacteristics& chars) {
   ATRACE_CALL();
+  if (VcamRenderRgb != nullptr &&
+      VcamRenderRgb(img, width, height, stride,
+                    static_cast<uint32_t>(layout))) {
+    return;
+  }
   float total_gain = gain / 100.0 * GetBaseGainFactor(chars.max_raw_value);
   // In fixed-point math, calculate total scaling from electrons to 8bpp
@@ -1490,4 +1511,11 @@ void EmulatedSensor::CaptureYUV420(YCbCrPlanes yuv_layout, uint32_t width,
                                    const SensorCharacteristics& chars) {
   ATRACE_CALL();
+  if (VcamRenderYuv420 != nullptr &&
+      VcamRenderYuv420(yuv_layout.img_y, yuv_layout.img_cb,
+                       yuv_layout.img_cr, width, height, yuv_layout.y_stride,
+                       yuv_layout.cbcr_stride, yuv_layout.cbcr_step,
+                       yuv_layout.bytesPerPixel)) {
+    return;
+  }
   float total_gain = gain / 100.0 * GetBaseGainFactor(chars.max_raw_value);
   // Using fixed-point math with 6 bits of fractional precision.
