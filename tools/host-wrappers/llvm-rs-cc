#!/usr/bin/env bash
set -euo pipefail

script_dir=$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)
compat_lib_dir=$(cd -- "$script_dir/../../../.." && pwd)/host-compat-libs
export LD_LIBRARY_PATH="$compat_lib_dir${LD_LIBRARY_PATH:+:$LD_LIBRARY_PATH}"
exec "$script_dir/llvm-rs-cc.real" "$@"
